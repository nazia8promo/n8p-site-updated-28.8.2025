export type CTA = {
  label: string;
  href: string;
};

export type CardItem = {
  title: string;
  text: string;
  href?: string;
};

export type StepItem = {
  step: string;
  title: string;
  text: string;
};
